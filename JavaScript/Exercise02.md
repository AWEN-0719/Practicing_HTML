透過這個練習來複習「資料 → 表格 → 圖表」的流程
- 用 JavaScript 動態產生表格，不用直接寫在 HTML。
- createElement() 可以建立 table、tr、td 等元素。
- append() / appendChild() 可以把元素加到網頁上。
- 使用 forEach() 可以快速跑過陣列資料。
- Object.values() 可以把物件裡的值轉成陣列。
- Chart.js 需要搭配 <canvas> 才能畫圖表。
- 圓餅圖的資料要分成 labels（名稱） 和 data（數值）。
- 將資料整理成陣列後再傳進函式，程式會比較好維護。
